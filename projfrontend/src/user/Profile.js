import React from 'react';
import Base from "../"
const Profile = () => {
    return (
        <Base title="Profile page">
        <h1>This is Profile page</h1>
        </Base>
    )
}

export default Profile;